import TabPlaceholder from "@/components/TabPlaceholder";

export default function Calendar() {
  return (
    <TabPlaceholder
      title="Calendar"
      subtitle="Plan around the sun — saved days and reminders will live here."
    />
  );
}
